BaseTemplate will be used by the plugin creator

BaseTemplate_Code is a template for an empty plugin with native code
BaseTemplate_Blueprint is " with only BP Content

The text PLUGIN_NAME will be replaced with the user's chosen name for new plugin
Includes:
	- File names
	- Text (native) file contents
	- uasset names